<div class="container">
  <div class="row">
    <div class="col-md-3" id="leftCol">
      <ul class="nav nav-stacked" id="sidebar">
        {{#each lang in this}}
        <li>
        {{#link-to "lex.entry" collection lang}}
          {{lang.name}}
        {{/link-to}}
        </li>
        {{/each}}
      </ul>
    </div>
    <div class="col-md-9">
      {{outlet}}
    </div>
  </div>
</div>
